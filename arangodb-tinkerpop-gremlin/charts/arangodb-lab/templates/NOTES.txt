1. Check pod status:
   kubectl get pods -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}

2. Port-forward ArangoDB if needed:
   kubectl port-forward -n {{ .Release.Namespace }} service/{{ include "arangodb-lab.fullname" . }} 8529:{{ .Values.service.port }}
