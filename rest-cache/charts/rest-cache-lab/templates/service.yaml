apiVersion: v1
kind: Service
metadata:
  name: {{ include "rest-cache-lab.fullname" . }}
  labels:
    {{- include "rest-cache-lab.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "rest-cache-lab.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
